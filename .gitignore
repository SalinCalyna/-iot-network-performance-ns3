# Python
__pycache__/
*.pyc
venv/
.venv/
myenv/

# Secrets -- never commit real credentials; dashboard/.env.example is the
# checked-in template, actual .env files are always local-only.
.env
*.env
!*.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/

# ns-3 build artifacts (not part of this repo -- this repo is not a full
# ns-3 checkout; scratch/iot-network.cc is meant to be dropped into one)
build/
cmake-cache/
*.pcap
*.log.tmp
